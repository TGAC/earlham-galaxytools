<tool id="aequatus" name="Aequatus" version="0.2.0">
    <description>generates datasets that can be visualised with Aequatus</description>
    <command>
<![CDATA[
python '$__tool_directory__/aequatus.py'
#for $dataset in $treeFile:
    -t '$dataset'
#end for
#for $dataset in $alignmentFile:
    -c '$dataset'
#end for
-g '$genesFile'
$sort
-o '$outputFile'
]]>
    </command>
    <inputs>
        <param name="treeFile" type="data" format="nhx" multiple="true" label="Gene tree" help="Gene tree in Newick format, e.g. generated from 'Get gene tree by Ensembl ID' or 'TreeBeST best' tool" />
        <param name="alignmentFile" type="data" format="tabular" multiple="true" label="CDS alignments" help="CDS alignments in tabular format generated by 'T-Coffee to CIGAR'" />

        <param name="genesFile" type="data" format="json" label="Gene features" help="Gene features in JSON format generated by 'GFF3 to JSON' or 'Get Ensembl features by ID' tool" />
        <param name="sort" type="boolean" truevalue="-s" falsevalue="" label="Sort the keys in the Aequatus JSON output" help="Ensure reproducibility, but can slow down the JSON creation for big files" />
    </inputs>
    <outputs>
        <data name="outputFile" format="sqlite" label="${tool.name} on ${on_string}" />
    </outputs>
    <tests>
        <test>
            <param name="treeFile" ftype="nhx" value="tree.nhx" />
            <param name="alignmentFile" ftype="tabular" value="cigar.tabular" />
            <param name="genesFile" ftype="json" value="gene.json" />
            <param name="sort" value="-s" />
            <output name="outputFile" ftype="sqlite" file="test.db" />
        </test>
    </tests>
    <help>
<![CDATA[
Simple tool to generate an SQLite database that can be visualised with `Aequatus`_.

Aequatus is an open-source homology browser developed with novel rendering approaches to visualise homologous, orthologous and paralogous gene structures.

.. _Aequatus: http://aequatus.earlham.ac.uk
]]>
    </help>
    <citations>
    </citations>
</tool>

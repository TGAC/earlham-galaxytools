<tool id="fasta_header_converter" name="Fasta header converter" version="0.1.1">
    <description>Adds species information at the end of the FASTA header</description>
    <command>
<![CDATA[
python $__tool_directory__/fasta_header_converter.py 
-f $fastaFile 
-j $genesFile
> $outputFile
]]>
    </command>
    <inputs>
        <param name="fastaFile" type="data" format="fasta" label="FASTA file" help="FASTA file with transcript ID as FASTA ID" />        
        <param name="genesFile" type="data" format="json" label="Gene file" help="Gene file in JSON format" />
    </inputs>
    <outputs>
        <data format="fasta" name="outputFile" label="${tool.name} on ${on_string}" />
    </outputs>
    <tests>
        <test>
            <param name="fastaFile" ftype="nhx" value="test.fasta" />
            <param name="genesFile" ftype="json" value="test.json" />
            <param name="outputFile" ftype="fasta" value="out.fasta" />
        </test>
    </tests>
    <help>
    <![CDATA[
Simple converter for FASTA, which appends species name in the FASTA header for usage in TreeBeST. It uses gene feature information in JSON format (similar to the result of Ensembl REST API - lookup/id). 
    ]]>
    </help>
    <citations>
    </citations>
</tool>
